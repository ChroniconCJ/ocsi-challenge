# Evaluation set
A held-back set of museum coins with verified type labels is reserved for scoring ML entrants (labels withheld; scoring by request while the leaderboard is assembled). Contact via the OCSI site. Scores will be published in LEADERBOARD.md.
